package com.feihua.framework.message.impl;

import com.feihua.framework.message.api.ApiBaseMessageClientPoService;
import com.feihua.framework.message.dto.BaseMessageClientDto;
import com.feihua.framework.message.po.BaseMessageClientPo;
import com.github.pagehelper.Page;
import feihua.jdbc.api.pojo.BasePo;
import feihua.jdbc.api.pojo.PageResultDto;
import feihua.jdbc.api.service.impl.ApiBaseServiceImpl;
import java.util.List;

import org.apache.commons.lang3.StringUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/**
 * This class was generated by MyBatis Generator.
 * @author yangwei 2019-05-08 09:18:54
 */
@Service
public class ApiBaseMessageClientPoServiceImpl extends ApiBaseServiceImpl<BaseMessageClientPo, BaseMessageClientDto, String> implements ApiBaseMessageClientPoService {
    @Autowired
    com.feihua.framework.message.mapper.BaseMessageClientPoMapper BaseMessageClientPoMapper;

    public ApiBaseMessageClientPoServiceImpl() {
        super(BaseMessageClientDto.class);
    }

    @Override
    public PageResultDto<BaseMessageClientDto> searchBaseMessageClientsDsf(com.feihua.framework.message.dto.SearchBaseMessageClientsConditionDto dto, feihua.jdbc.api.pojo.PageAndOrderbyParamDto pageAndOrderbyParamDto) {
        Page p = super.pageAndOrderbyStart(pageAndOrderbyParamDto);
        List<com.feihua.framework.message.dto.BaseMessageClientDto> list = this.wrapDtos(BaseMessageClientPoMapper.searchBaseMessageClients(dto));
        return new PageResultDto(list, this.wrapPage(p));
    }

    @Override
    public List<BaseMessageClientDto> selectByClientIdAndIsEnable(String clientId,String isEnable) {
        if (StringUtils.isAnyEmpty(clientId,isEnable)) return null;
        BaseMessageClientPo messageClientPo = new BaseMessageClientPo();
        messageClientPo.setClientId(clientId);
        messageClientPo.setIsEnable(isEnable);
        messageClientPo.setDelFlag(BasePo.YesNo.N.name());
        return selectList(messageClientPo);
    }

    @Override
    public BaseMessageClientDto wrapDto(BaseMessageClientPo po) {
        if (po == null) { return null; }
        BaseMessageClientDto dto = new BaseMessageClientDto();
        dto.setId(po.getId());
        dto.setClientId(po.getClientId());
        dto.setMessageType(po.getMessageType());
        dto.setIsEnable(po.getIsEnable());
        dto.setDataUserId(po.getDataUserId());
        dto.setDataOfficeId(po.getDataOfficeId());
        dto.setDataType(po.getDataType());
        dto.setDataAreaId(po.getDataAreaId());
        dto.setUpdateAt(po.getUpdateAt());
        return dto;
    }
}
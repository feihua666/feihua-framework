package com.feihua.framework.message.impl;

import com.feihua.framework.message.api.ApiBaseMessageUserPoService;
import com.feihua.framework.message.dto.BaseMessageUserDto;
import com.feihua.framework.message.po.BaseMessageUserPo;
import com.github.pagehelper.Page;
import feihua.jdbc.api.pojo.PageResultDto;
import feihua.jdbc.api.service.impl.ApiBaseServiceImpl;
import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/**
 * This class was generated by MyBatis Generator.
 * @author yangwei 2019-04-25 17:26:51
 */
@Service
public class ApiBaseMessageUserPoServiceImpl extends ApiBaseServiceImpl<BaseMessageUserPo, BaseMessageUserDto, String> implements ApiBaseMessageUserPoService {
    @Autowired
    com.feihua.framework.message.mapper.BaseMessageUserPoMapper BaseMessageUserPoMapper;

    public ApiBaseMessageUserPoServiceImpl() {
        super(BaseMessageUserDto.class);
    }

    @Override
    public BaseMessageUserDto wrapDto(BaseMessageUserPo po) {
        if (po == null) { return null; }
        BaseMessageUserDto dto = new BaseMessageUserDto();
        dto.setId(po.getId());
        dto.setMessageId(po.getMessageId());
        dto.setUserId(po.getUserId());
        dto.setIsRead(po.getIsRead());
        dto.setIsCanRead(po.getIsCanRead());
        dto.setReadClientId(po.getReadClientId());
        dto.setReadTime(po.getReadTime());
        dto.setDataUserId(po.getDataUserId());
        dto.setDataOfficeId(po.getDataOfficeId());
        dto.setDataType(po.getDataType());
        dto.setDataAreaId(po.getDataAreaId());
        dto.setUpdateAt(po.getUpdateAt());
        return dto;
    }
}
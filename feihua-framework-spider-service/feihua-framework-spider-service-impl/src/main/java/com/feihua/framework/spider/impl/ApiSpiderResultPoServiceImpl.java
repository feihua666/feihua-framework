package com.feihua.framework.spider.impl;

import com.feihua.framework.spider.api.ApiSpiderResultPoService;
import com.feihua.framework.spider.dto.SpiderResultDto;
import com.feihua.framework.spider.mapper.SpiderResultPoMapper;
import com.feihua.framework.spider.po.SpiderResultPo;
import feihua.jdbc.api.pojo.BasePo;
import feihua.jdbc.api.service.impl.ApiBaseServiceImpl;
import org.apache.commons.lang3.StringUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/**
 * This class was generated by MyBatis Generator.
 * @author yangwei 2018-12-19 17:20:07
 */
@Service
public class ApiSpiderResultPoServiceImpl extends ApiBaseServiceImpl<SpiderResultPo, SpiderResultDto, String> implements ApiSpiderResultPoService {
    @Autowired
    SpiderResultPoMapper SpiderResultMapper;

    public ApiSpiderResultPoServiceImpl() {
        super(SpiderResultDto.class);
    }

    @Override
    public SpiderResultPo selectByUrl(String url) {
        if(StringUtils.isEmpty(url)) return null;
        SpiderResultPo spiderResultPo = new SpiderResultPo();
        spiderResultPo.setUrl(url);
        spiderResultPo.setDelFlag(BasePo.YesNo.N.name());
        return this.selectOneSimple(spiderResultPo);
    }
    @Override
    public SpiderResultDto wrapDto(SpiderResultPo po) {
        if (po == null) { return null; }
        SpiderResultDto dto = new SpiderResultDto();
        dto.setId(po.getId());
        dto.setConfigSourceId(po.getConfigSourceId());
        dto.setUrl(po.getUrl());
        dto.setContentType(po.getContentType());
        dto.setTitle(po.getTitle());
        dto.setParentId(po.getParentId());
        dto.setDataUserId(po.getDataUserId());
        dto.setDataOfficeId(po.getDataOfficeId());
        dto.setDataType(po.getDataType());
        dto.setDataAreaId(po.getDataAreaId());
        dto.setUpdateAt(po.getUpdateAt());
        dto.setDocument(po.getDocument());
        return dto;
    }

}
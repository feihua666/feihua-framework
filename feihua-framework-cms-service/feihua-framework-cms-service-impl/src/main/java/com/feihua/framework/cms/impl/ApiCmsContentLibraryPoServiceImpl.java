package com.feihua.framework.cms.impl;

import com.feihua.framework.cms.api.ApiCmsContentLibraryPoService;
import com.feihua.framework.cms.dto.CmsContentLibraryDto;
import com.feihua.framework.cms.po.CmsContentLibraryPo;
import feihua.jdbc.api.pojo.BasePo;
import feihua.jdbc.api.service.impl.ApiBaseServiceImpl;
import org.apache.commons.lang3.StringUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/**
 * This class was generated by MyBatis Generator.
 * @author yangwei 2018-12-06 15:42:29
 */
@Service
public class ApiCmsContentLibraryPoServiceImpl extends ApiBaseServiceImpl<CmsContentLibraryPo, CmsContentLibraryDto, String> implements ApiCmsContentLibraryPoService {
    @Autowired
    com.feihua.framework.cms.mapper.CmsContentLibraryPoMapper CmsContentLibraryPoMapper;

    public ApiCmsContentLibraryPoServiceImpl() {
        super(CmsContentLibraryDto.class);
    }

    @Override
    public CmsContentLibraryPo selectBySiteIdAndContentId(String siteId, String contentId) {
        if(StringUtils.isNotEmpty(siteId)){
            CmsContentLibraryPo libraryPo = new CmsContentLibraryPo();
            libraryPo.setSiteId(siteId);
            libraryPo.setContentId(contentId);
            libraryPo.setDelFlag(BasePo.YesNo.N.name());
            return this.selectOneSimple(libraryPo);
        }
        return null;
    }
    @Override
    public CmsContentLibraryDto wrapDto(CmsContentLibraryPo po) {
        if (po == null) { return null; }
        CmsContentLibraryDto dto = new CmsContentLibraryDto();
        dto.setId(po.getId());
        dto.setSiteId(po.getSiteId());
        dto.setContentId(po.getContentId());
        dto.setFilename(po.getFilename());
        dto.setUrl(po.getUrl());
        dto.setExt(po.getExt());
        dto.setType(po.getType());
        dto.setDwonloadNum(po.getDwonloadNum());
        dto.setSize(po.getSize());
        dto.setDescription(po.getDescription());
        dto.setImageUrl(po.getImageUrl());
        dto.setImageDes(po.getImageDes());
        dto.setSequence(po.getSequence());
        dto.setDataUserId(po.getDataUserId());
        dto.setDataOfficeId(po.getDataOfficeId());
        dto.setDataType(po.getDataType());
        dto.setDataAreaId(po.getDataAreaId());
        dto.setUpdateAt(po.getUpdateAt());
        return dto;
    }

}
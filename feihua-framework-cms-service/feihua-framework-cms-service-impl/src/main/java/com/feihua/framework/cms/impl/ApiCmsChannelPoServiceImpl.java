package com.feihua.framework.cms.impl;

import com.feihua.framework.cms.api.ApiCmsChannelPoService;
import com.feihua.framework.cms.dto.CmsChannelDto;
import com.feihua.framework.cms.po.CmsChannelPo;
import com.github.pagehelper.Page;
import feihua.jdbc.api.pojo.BasePo;
import feihua.jdbc.api.pojo.PageResultDto;
import feihua.jdbc.api.service.impl.ApiBaseServiceImpl;
import java.util.List;

import feihua.jdbc.api.service.impl.ApiBaseTreeServiceImpl;
import org.apache.commons.lang3.StringUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/**
 * This class was generated by MyBatis Generator.
 * @author yangwei 2018-11-09 16:45:29
 */
@Service
public class ApiCmsChannelPoServiceImpl extends ApiBaseTreeServiceImpl<CmsChannelPo, CmsChannelDto, String> implements ApiCmsChannelPoService {
    @Autowired
    com.feihua.framework.cms.mapper.CmsChannelPoMapper CmsChannelPoMapper;

    public ApiCmsChannelPoServiceImpl() {
        super(CmsChannelDto.class);
    }

    @Override
    public PageResultDto<CmsChannelDto> searchCmsChannelsDsf(com.feihua.framework.cms.dto.SearchCmsChannelsConditionDto dto, feihua.jdbc.api.pojo.PageAndOrderbyParamDto pageAndOrderbyParamDto) {
        Page p = super.pageAndOrderbyStart(pageAndOrderbyParamDto);
        List<com.feihua.framework.cms.dto.CmsChannelDto> list = this.wrapDtos(CmsChannelPoMapper.searchCmsChannels(dto));
        return new PageResultDto(list, this.wrapPage(p));
    }

    @Override
    public CmsChannelPo selectByPathAndSiteId(String channelPath, String siteId) {

        if(StringUtils.isAnyEmpty(channelPath,siteId)){
            return null;
        }
        CmsChannelPo cmsChannelPo = new CmsChannelPo();
        cmsChannelPo.setPath(channelPath);
        cmsChannelPo.setSiteId(siteId);
        cmsChannelPo.setDelFlag(BasePo.YesNo.N.name());
        return this.selectOneSimple(cmsChannelPo);
    }

    @Override
    public CmsChannelDto wrapDto(CmsChannelPo po) {
        if (po == null) { return null; }
        CmsChannelDto dto = new CmsChannelDto();
        dto.setId(po.getId());
        dto.setName(po.getName());
        dto.setPath(po.getPath());
        dto.setSequence(po.getSequence());
        dto.setSiteId(po.getSiteId());
        dto.setPv(po.getPv());
        dto.setIv(po.getIv());
        dto.setUv(po.getUv());
        dto.setLevel(po.getLevel());
        dto.setParentId(po.getParentId());
        dto.setDataUserId(po.getDataUserId());
        dto.setDataOfficeId(po.getDataOfficeId());
        dto.setDataType(po.getDataType());
        dto.setDataAreaId(po.getDataAreaId());
        dto.setUpdateAt(po.getUpdateAt());
        dto.setChannelType(po.getChannelType());
        dto.setTemplate(po.getTemplate());
        return dto;
    }
}
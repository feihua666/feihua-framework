package com.feihua.framework.cms.impl;

import com.feihua.framework.cms.api.ApiCmsContentDownloadPoService;
import com.feihua.framework.cms.dto.CmsContentDownloadDto;
import com.feihua.framework.cms.po.CmsContentDownloadPo;
import feihua.jdbc.api.pojo.BasePo;
import feihua.jdbc.api.service.impl.ApiBaseServiceImpl;
import org.apache.commons.lang3.StringUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/**
 * This class was generated by MyBatis Generator.
 * @author yangwei 2018-12-12 12:28:18
 */
@Service
public class ApiCmsContentDownloadPoServiceImpl extends ApiBaseServiceImpl<CmsContentDownloadPo, CmsContentDownloadDto, String> implements ApiCmsContentDownloadPoService {
    @Autowired
    com.feihua.framework.cms.mapper.CmsContentDownloadPoMapper CmsContentDownloadPoMapper;

    public ApiCmsContentDownloadPoServiceImpl() {
        super(CmsContentDownloadDto.class);
    }

    @Override
    public CmsContentDownloadPo selectBySiteIdAndContentId(String siteId, String contentId) {
        if(StringUtils.isAnyEmpty(siteId,contentId)){
            return null;
        }
        CmsContentDownloadPo cmsContentDownloadPo = new CmsContentDownloadPo();
        cmsContentDownloadPo.setSiteId(siteId);
        cmsContentDownloadPo.setContentId(contentId);
        cmsContentDownloadPo.setDelFlag(BasePo.YesNo.N.name());
        return selectOneSimple(cmsContentDownloadPo);

    }
    @Override
    public CmsContentDownloadDto wrapDto(CmsContentDownloadPo po) {
        if (po == null) { return null; }
        CmsContentDownloadDto dto = new CmsContentDownloadDto();
        dto.setId(po.getId());
        dto.setSiteId(po.getSiteId());
        dto.setContentId(po.getContentId());
        dto.setFilename(po.getFilename());
        dto.setUrl(po.getUrl());
        dto.setExt(po.getExt());
        dto.setType(po.getType());
        dto.setDwonloadNum(po.getDwonloadNum());
        dto.setSize(po.getSize());
        dto.setDescription(po.getDescription());
        dto.setImageUrl(po.getImageUrl());
        dto.setImageDes(po.getImageDes());
        dto.setSequence(po.getSequence());
        dto.setDataUserId(po.getDataUserId());
        dto.setDataOfficeId(po.getDataOfficeId());
        dto.setDataType(po.getDataType());
        dto.setDataAreaId(po.getDataAreaId());
        dto.setUpdateAt(po.getUpdateAt());
        dto.setOs(po.getOs());
        dto.setOfficialName(po.getOfficialName());
        dto.setOfficialUrl(po.getOfficialUrl());
        dto.setUpdateTime(po.getUpdateTime());
        dto.setLanguage(po.getLanguage());

        return dto;
    }

}